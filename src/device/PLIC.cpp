#include "device/PLIC.h"

PLIC::PLIC() {
    // Constructor
}

PLIC::~PLIC() {
    // Destructor
}

bool PLIC::read(size_t addr, size_t size, uint8_t* buff) {
    // Read from PLIC
    return false;
}

bool PLIC::write(size_t addr, size_t size, uint8_t* buff) {
    // Write to PLIC
    return false;
}
